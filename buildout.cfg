[buildout]
parts = python test
develop = .
eggs = irgsh

[python]
recipe = zc.recipe.egg
eggs = ${buildout:eggs}
interpreter = python

[test]
recipe = pbp.recipe.noserunner
eggs = ${buildout:eggs}
    mock
    coverage

